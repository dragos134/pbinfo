#include <fstream>
#include <cstring>
using namespace std;

ifstream fin("coada1.in");
ofstream fout("coada1.out");

int main()
{
    int a[1000], n, x;
    string cmd;
    fin >> n;
    while(n)
    {
        n--;
        fin >> cmd >> x;
        
    }
    return 0;
}